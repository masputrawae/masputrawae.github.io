---
import type { HTMLAttributes } from 'astro/types'

type Props = HTMLAttributes<'div'>

const { class: className, ...attr } = Astro.props
---

<div class:list={['container px-4 mx-auto', className]} {...attr}>
  <slot />
</div>
